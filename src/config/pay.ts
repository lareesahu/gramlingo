/* ═══════════════════════════════════════════════
   GRAMLINGO — Payments endpoints (pulse-pay)
   Hosted pages served by the shared pulse-pay
   service. Link out only — never embed secrets or
   card data in the app.
   ═══════════════════════════════════════════════ */

/** Hosted pricing page scoped to GramLingo products (world-1 $9, worlds-all $29, pro-yearly $49). */
export const PAY_PRICING_URL = 'https://pulse-pay-97j0.onrender.com/pricing?site=gramlingo';

/** Account page — sign in with the buyer email to see what is already unlocked. */
export const PAY_ACCOUNT_URL = 'https://pulse-pay-97j0.onrender.com/account';
